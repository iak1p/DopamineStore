services:
  db:
    image: postgres:16
    container_name: eshop_db
    environment:
      POSTGRES_DB: eshop
      POSTGRES_USER: eshop
      POSTGRES_PASSWORD: eshop
    ports:
      - "5434:5432"
    volumes:
      - eshop_pg:/var/lib/postgresql/data

volumes:
  eshop_pg: